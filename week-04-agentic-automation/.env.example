# =============================================================================
# MCP Hub — centralised environment variables
# Copy this file to .env and fill in your real values.
# NEVER commit .env to git. Add it to .gitignore.
# =============================================================================

# ── NetBox MCP ───────────────────────────────────────────────────────────────
# URL reachable FROM the container. Use host.docker.internal for a NetBox
# instance running on your host (not in this compose stack).
NETBOX_URL=http://host.docker.internal:8000

# API token shared by NetBox MCP and pyATS vault integration.
NETBOX_TOKEN=your_generated_token_here

# Host and port the netbox-mcp container listens on.
NETBOX_MCP_HOST=0.0.0.0
NETBOX_MCP_PORT=8080

# Optional tuning (defaults shown).
# NETBOX_MCP_LOG_LEVEL=INFO      # Set to DEBUG for verbose output
# NETBOX_MCP_VERIFY_SSL=true     # Set to false for self-signed certs

# ── pyATS ────────────────────────────────────────────────────────────────────
PYATS_MCP_TRANSPORT=http
PYATS_MCP_HOST=0.0.0.0
PYATS_MCP_PORT=8081
PYATS_MCP_KEEP_ARTIFACTS=1

# Vault integration — base URL reachable from the pyats-mcp container.
# Matches NETBOX_URL but kept separate in case you point to a different vault.
CREDENTIALS_VAULT_BASE_URL=http://host.docker.internal:8000
CREDENTIALS_VAULT_USERNAME_ROLE=username
CREDENTIALS_VAULT_PASSWORD_ROLE=password
# Set to "0" to disable SSL verification (e.g. self-signed certs).
CREDENTIALS_VAULT_VERIFY_SSL=1

# ── GitHub MCP ───────────────────────────────────────────────────────────────
# Create a fine-grained PAT at https://github.com/settings/tokens
GITHUB_PAT=your_generated_token_here

# Host and port the github-mcp container listens on.
GITHUB_MCP_HOST=0.0.0.0
GITHUB_MCP_PORT=8082

# Comma-separated list of toolsets to expose. Remove any you don't need.
# Available: repos, issues, pull_requests, actions, code_security,
#            projects, discussions, users, search, notifications
GITHUB_TOOLSETS=repos,issues,pull_requests,actions

# Uncomment for GitHub Enterprise:
# GITHUB_HOST=github.your-company.com

# ── DrawIO MCP ───────────────────────────────────────────────────────────────
DRAWIO_MCP_HOST=0.0.0.0
DRAWIO_MCP_PORT=8083
